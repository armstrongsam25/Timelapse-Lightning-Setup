# picamera2 is NOT pip-installable on Raspberry Pi OS — install via apt instead:
#   sudo apt install -y python3-picamera2
# Do not add it to this file or pip will pull source and fail on libcap headers.

# Future (sensor phase): serial reader for the Arduino event stream
pyserial
